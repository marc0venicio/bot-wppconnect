version: '3.8'

services:
  wppbot:
    build: .
    container_name: wppconnect-bot
    volumes:
      - .:/app
      - ./tokens:/app/tokens
    environment:
      - TZ=America/Sao_Paulo
    ports:
      - "21465:21465"
    restart: always
